export interface TeamMember {
    id: number;
    firstName: string;
    lastName: string;
    email?: string; // Optional
    phoneNumber?: string; // Optional
    university?: string; // Optional
    dateOfBirth?: string; // Optional
    // Add any other relevant fields that you expect to receive
  }
  